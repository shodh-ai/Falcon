'use client';

import NotificationsPage from '@/app/notifications/page';

/** Faculty-scoped entry for the shared Falcon notification center. */
export default function FacultyNotificationsPage() {
  return <NotificationsPage />;
}
