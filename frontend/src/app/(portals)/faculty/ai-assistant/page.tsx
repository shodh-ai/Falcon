import { ComingSoonWorkspace } from '@/components/shared/ComingSoonWorkspace';

export default function FacultyAiAssistantPage() {
  return (
    <ComingSoonWorkspace
      title="Faculty AI Assistant"
      description="The assistant is being prepared for a future controlled release. It is not operational and cannot process faculty or student information yet."
      backHref="/faculty/dashboard"
    />
  );
}
