/** BookMyShow-style bed checkout window (seconds). */
export const BED_LOCK_TTL_SEC = 180;

/** Grace after Redis TTL for delayed payment webhooks (seconds). */
export const BED_LOCK_GRACE_SEC = 30;
